<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="14dp"
    android:background="@drawable/widget_background">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="&#128203; Планы на сегодня"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="@color/widget_text"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_toStartOf="@id/widget_add_btn_frame"
        android:layout_marginEnd="8dp" />

    <FrameLayout
        android:id="@+id/widget_add_btn_frame"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true">

        <ImageView
            android:id="@+id/widget_add_bg"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@drawable/widget_add_bg"
            android:scaleType="fitXY" />

        <ImageView
            android:id="@+id/widget_add_btn"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:src="@android:drawable/ic_input_add"
            android:padding="7dp"
            android:scaleType="centerInside" />

    </FrameLayout>

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_title"
        android:layout_marginTop="16dp"
        android:textSize="13sp"
        android:textColor="@color/widget_subtext"
        android:text="Нет планов на сегодня"
        android:visibility="gone" />

    <ListView
        android:id="@+id/widget_plans_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/widget_title"
        android:layout_marginTop="6dp"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:scrollbars="none"
        android:listSelector="@android:color/transparent"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:visibility="gone" />

</RelativeLayout>
